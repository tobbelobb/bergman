// railway.eliza.json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE" },
  "deploy": { "startCommand": "pnpm start --non-interactive" }
}

// railway.client.json
{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "rootDirectory": "client" },
  "deploy": {
    "startCommand": "pnpm start:client --host 0.0.0.0 --port 5173",
    "sleepApplication": false
  }
}
